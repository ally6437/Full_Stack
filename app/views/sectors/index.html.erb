<div class="ml-6">
<h1 class="title is-2">Sectors</h1>
<ol>

  <% @sectors.each do |i| %>
  <li>
    <%= link_to i.name, i  %>
    <em>(<%= pluralize i.company_count, "Company" %>)</em>
  </li>
  <%end%>
</ol>
</div>
